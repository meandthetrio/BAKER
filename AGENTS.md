# Agent notes (ADSR_V2)

Firmware for Daisy Pod: layered `AppState`, UI under `src/ui/`, worker/SD under `src/worker/`, voice engine at repo root. Architecture map: `docs/START_HERE.md`. Build: `make` from this directory.
